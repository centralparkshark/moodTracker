<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" type="text/css" href="../style.css" />
</head>
<body>
    <header>
        <a href="/users">Users</a>
        <a href="/moods">Moods</a>
        <a href="/journal">Journal</a>
    </header>
    <div class="main">
        <h1><%= title %></h1>
        <div class="container">
            <% if (items.length > 0) {items.forEach(function(item) { %>
                <a href="/<%= item.link %>"><div class="item">
                    <img src="<%= item.img %>" alt="">
                    <div class="info">
                        <h2><%= item.name %></h2>
                        <p><%= item.username %></p>
                        <p><%= item.description %></p>
                    </div>
                </div></a>
            <% })} else { %>
                <h2 style="text-align: center;">Click Link Above</h2>
                <% }; %>
        </div>
    </div>
    
</body>
</html>